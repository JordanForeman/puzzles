import org.junit.Test;
import static org.junit.Assert.assertEquals;

public class MainTest {
    @Test
    public void testSolve() {
        String input = "test input";
        String expected = "";
        assertEquals(expected, Main.solve(input));
    }
}
