/**
 * The following method takes two previously sorted arrays and combines them
 * It assumes the following:
 *
 * 1. `arr1` is right-padded with zeroes equal to the length of `arr2`
 * 2. Both `arr1` and `arr2` have been previously sorted
 */
export function merge(arr1, arr2) {
  return [1, 2, 3, 4, 5, 6];
}
